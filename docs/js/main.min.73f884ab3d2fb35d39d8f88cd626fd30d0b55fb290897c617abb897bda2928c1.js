console.log("Asset pipeline JavaScript loaded!");const gallery=document.querySelector(".auto-scroll-gallery");let isUserInteracting=!1,scrollSpeed=.5;gallery.addEventListener("wheel",()=>isUserInteracting=!0),gallery.addEventListener("touchstart",()=>isUserInteracting=!0),gallery.addEventListener("mouseenter",()=>isUserInteracting=!0);function autoScroll(){isUserInteracting||(gallery.scrollLeft+=scrollSpeed,gallery.scrollLeft+gallery.clientWidth>=gallery.scrollWidth&&(gallery.scrollLeft=0)),requestAnimationFrame(autoScroll)}autoScroll()